version: '2'

services:

  registrator:
    image: gliderlabs/registrator:master
    command: "-ip {{ consul_server_ip }} consul://{{ consul_server_ip }}:8500"
    container_name: {{ service_name }}
    network_mode: host
    volumes:
    - /var/run/docker.sock:/tmp/docker.sock